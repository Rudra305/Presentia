
# Public env vars — anything a mobile client sees at build/runtime must be prefixed EXPO_PUBLIC_
# Never put real secrets here. Real secrets belong in EAS Secrets or the (future) backend.

EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=

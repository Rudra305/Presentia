import { PlaceholderScreen } from '@/core/ui/templates/PlaceholderScreen';

export default function SharedSettingsScreen() {
    return (
        <PlaceholderScreen
            icon="sliders"
            title="Settings"
            subtitle="Theme, language, notifications"
            testID="shared-settings"
        />
    );
}
